version: "3.9"

services:

  app:

    build: .

    command: npm test

    depends_on:
      - postgres
      - redis

  postgres:

    image: postgres:15

    environment:
      POSTGRES_DB: notification_engine
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123

  redis:

    image: redis:7